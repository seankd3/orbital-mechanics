# Archived specs

Design specs from the pre-v4 Apollo build: checklists, failures, telemetry,
entry and rendezvous. They are kept for reference only. The current design is
[`DESIGN.md`](../../DESIGN.md). Where a spec here conflicts with it (for
example the checklist-gated commands or the DSKY flow), `DESIGN.md` wins.
